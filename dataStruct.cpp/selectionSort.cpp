loop through and locate smallest item in the list
swap first index with the smallest value
set minValue to next unsorted value and repeat the loop that loops through to find minValue
swap next unsorted value with the minValue 


int main()
{
  return 0;
}
